{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Main window: core + events from Rust (snapshots, link state)",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
